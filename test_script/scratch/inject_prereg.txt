Injection-response test, formC_b arm best / ap_src post, Meng 10 s ramp
(h 15 -> 2.5 um, w_bar 6.667 -> 1.111, no oscillation), 8 seeds paired,
+-5 % multiplicative injection into posterior a_bar(z) at t = 5.5 s (w_bar_d ~ 3.9).
Written 2026-08-26 BEFORE the run.

P1  r(t) = (a_hat_inj - a_hat_base)/injected decays toward 0 within ~1-2 s
    (y1 is informative on the ramp, v_d != 0).
P2  [hypothesis] the -5 % arm corrects harder (initial |dr/dt| ratio -/+ > 1)
    and overshoots (min r < 0). Mechanism: F_e(4,3) = (1-lc)*b*(1-a_hat)^2 is
    larger when a_hat is LOW, so P(4,1) and K1(4) are larger for a low estimate.
P3  [opponent] the two arms are mirror images and neither crosses 0
    => path gain 1, the +18 % bias is NOT created by this asymmetry; next target
    is the hold-segment innov_y1 mean (-0.045 nm, t = -2.4 on canonical).
P4  all three arms reach the same end-hold bias within the paired SEM
    (the attractor is set by the model, not by the history). If they differ,
    the bias carries path memory and the diagnosis changes.
Negative controls: N1 canonical seed 7 hook-off a_bar_hat_z[end] = 0.107505;
                   N2 hook firing with frac 0 bit-identical to hook off.
Reading rule: percentages on this ramp are NOT comparable to canonical deep.

--- Discriminating arm, 2026-08-26 (written before the run) ---
Same injection test with ctrl_const.fe43_off = true on ALL three arms
(law coupling F_e(4,3) = (1-lc)*a_bar' zeroed in the covariance propagation).
Hypothesis: near the wall P(4,1)'s sign is set by F_e(4,3) (law: higher particle
=> higher gain), so a control-induced lag is read as a gain increase.
P5  with F_e(4,3) = 0, r(t) peaks < 1.2 and decays (y1 starts correcting);
    the y1 response ledger turns NEGATIVE.
P6  [opponent] r still grows to ~2.4 => the amplification is not through F_e(4,3);
    check the innov/dw sign convention next.
Note: the baseline itself changes on this arm (P(4,1) built differently), so the
baseline trough bias is reported too; percentages are Meng-ramp, not canonical.

--- Discriminating arm 2, 2026-08-26 (written before the run) ---
Result of arm 1 (fe43_off): amplification WORSE (peak 3.0, K1 -0.24), so the
law coupling is not the sign source; canonical data show P41 > 0 on the fast
descent and < 0 in oscillation/hold, i.e. the negative sign appears where the
applied force is small and a' is large. Candidate source: the thermal
cross-noise Q(3,4) = -a'*Q33 (and its MA(2) memory route F_aug(4,8:9)).
Arm: ctrl_const.q34_off = true on all three arms (Q(3,4), Q(4,8), F_aug(4,8:9)
zeroed in P propagation only).
P7  K1(4) on the ramp turns >= 0; r(t) peaks < 1.3 and decays; y1 response
    ledger turns negative. Baseline honesty spread/sqrtP moves away from 1
    (the removed coupling is real physics -- this is a diagnostic, not a fix).
P8  [opponent] K1(4) stays negative and r still grows => the sign source is
    elsewhere (next: the sign of F_dw_det vs the dw = pd - p convention).

--- VERDICTS (2026-08-26, after all three arms) ---
P1 FAIL  P2 half (asymmetry ratio 1.13, gain route corrective -0.2, no overshoot)
P3 FAIL (gain negative, not 1)  P4 FAIL (path memory 24 %)
P5 FAIL / P6 HOLDS: fe43_off -> peak 3.0, K1 -0.24 (law coupling was helping)
P7 HOLDS: q34_off -> K1 +0.075, peak 1.1, y1 ledger -0.48/-0.46, arms converge (+-0.07 %);
          baseline honesty 0.99 -> 2.15 as predicted (the removed coupling is real physics)

--- Discriminating arm 3 (reverse), 2026-08-26 (written before the run) ---
Keep Q(3,4); scale Q(4,4) by N in {10, 100} (ctrl_const.q44_scale), all three arms.
P9  with N large enough, F_e(3,4)*P44 overtakes Q(3,4): K1(4) on the ramp turns >= 0,
    r(t) peak falls below 1.3 and decays, y1 ledger negative. Honesty spread/sqrtP
    drops BELOW 1 (P over-inflated: this is a diagnostic, not a fix).
P10 [opponent] K1(4) stays negative and r still amplifies at N = 100 => the sign is
    not a P44 competition; re-examine F_dw's sign convention.
P9 HOLDS (2026-08-26): q44_scale 10 -> K1 +0.025, r 0.62 @1 s, no overshoot, arms converge;
          100 -> K1 +0.091, r 0.28 @1 s. Honesty 0.64 / 0.55 (P over-inflated, as predicted).
          Main corrector becomes y2 (-1.4), y1 merely stops amplifying (-0.08); predict still +0.5.

--- Step 1: lever test on the TARGET metrics, 2026-08-26 (written before the run) ---
sweep_q44_scale on BOTH the Meng ramp (user's test bed) and canonical deep (where the
+18 % symptom lives), q44_scale in {1, 3, 10}, 8 seeds paired. Metrics: end-hold bias,
spread/sqrtP44 (honesty), per-seed sd, sign of K1(4).
P11 canonical: bias falls from ~+18 % toward < +8 % at scale 3-10 WHILE honesty stays in
    0.8-1.2 for at least one scale => lever exists, derivation is worth it.
P12 [opponent] bias does not move, or honesty leaves 0.8-1.2 before bias moves => the
    mechanism is real but its lever on the target is small; go back to the law itself.
P13 Meng: baseline bias is already small (+3.8 +- 2.4); expect K1(4) to turn >= 0 and the
    per-seed sd to change, bias change within SEM.
VERDICT step 1 (2026-08-26): canonical hold-end bias +22.65 -> +6.89 (x3) / +8.12 (x10),
  paired -15.75+-3.09 % (t=-5.1) / -14.52+-3.13 % (t=-4.6)  => lever EXISTS (P11 bias part).
  Honesty at hold end 1.02 -> 0.71 / 0.65 and per-seed sd 7.8 -> 12.0 / 11.2 % => uniform
  scaling over-inflates P (P11 honesty part FAILS at both scales). K1(4) in the hold stays
  -0.49/-0.47/-0.46: with F_dw ~ 0 the force route is zero whatever P44 is, so the sign
  flip happens on the crossings (descend K1 +0.04 -> +0.14 -> +0.23), not in the hold.
  Meng: -5.2+-3.1 (t=-1.7) / -3.0+-3.0, within SEM as P13 predicted; K1 on ramp -0.13 -> -0.02.
  Reading: a uniform x N also inflates P44 in the hold where the law-error term should be
  ZERO (dw_d = 0). A derived budget ~ (db*(1-a)^2*|dw|)^2 adds nothing in the hold, so the
  prediction for the derivation is: bias reduction kept, hold honesty ~1, smaller sd cost.

--- Route A = consider-b, VERDICT (2026-08-27, compare_consider_b + inject _consider) ---
A1 PASS  A2 PASS (b_hat = 8/9 exact, sqrtP55 = b_half exact)  A6 PASS (descend K1 +0.039 -> +0.070)
A3 FAIL  trough bias +22.65 -> +10.09 % (paired -12.55 +- 2.53, t = -5.0), target < 8 %
A4 FAIL  total RMS/sqrtP44 2.87 -> 1.73, target [0.3, 1.2]
A5 FAIL  per-seed sd 7.8 -> 13.3 %, target < 12 %
Prediction miss: sqrtP44 at trough 0.0087 vs registered 0.038 (first-order ignores updates:
ratio to prediction hold-start 0.998, descend 0.963, oscillate 0.452, hold-end 0.215).
Meng injection with consider_b: peak 2.1 (vs 2.4), y1 ledger +1.6/+2.1, K1 -0.121 -- unchanged.
Reading: the consider correlation P45 is consumed by the y1 updates whose sign is the defect;
the law error must re-enter every step as process noise (correlated container), not as a
one-time correlation. The q44 x3 arm's extra injection matches the correlated white term
(Delta_b (1-a)^2 dw)^2 x 12.7^2 at a = 0.5 within 10 %.

--- Level 2: correlated law-error container q_law, 2026-08-27 (written before the run) ---
q_law[k] = P_bb g (2 G_{k-1} + g), g = F_e(4,5), G = running sum inside one monotone command
segment (reset at dw_d = 0 or a sign change); added to Q44 only, not to R2's delay term.
Arms: best | best+lawq | bmid+lawq | best+lawq a_cov x2 | /2 ; Meng injection with lawq.
P-L2-1 trough bias in [+5, +10] %      (from +22.65)
P-L2-2 TOTAL RMS/sqrtP44, end hold, in [0.8, 1.5]   (x3 arm 0.82, consider 1.73)
P-L2-3 per-seed sd <= 12 %
P-L2-4 descend K1(4) >= +0.07 ; hold K1 unchanged (-0.49, structural)
P-L2-5 Meng injection: r peak < 1.5 and decays; y1 ledger <= 0; K1 >= 0 on the second half
P-L2-6 a_cov x2 / /2: bias moves < 1 pp, honesty < 0.1
P-L2-7 flag off: fixture 0.107505 bit-for-bit; shallow bit-for-bit
Reading: TOTAL/sP < 0.5 => bound too loose (sign cancellation) => route B;
         bias > 12 % with honesty ~ 1 => container honest, bias is not the law's.

--- Level 2 VERDICT (2026-08-27, compare_law_err_q + inject _lawq) ---
P-L2-1 PASS  +9.19 % (paired -13.46 +- 3.21, t = -4.2)      P-L2-4 PASS  descend K1 +0.158 (crossings up to +0.8)
P-L2-6 PASS  a_cov x2 / /2: bias +0.02 / -0.01 pp, honesty +0.07 / -0.04   P-L2-7 PASS (fixture, shallow bit-for-bit)
P-L2-2 FAIL  TOTAL/sP 1.64 (spread/sP alone 1.42: P44 0.0098 vs spread 0.0133 in the hold)
P-L2-3 FAIL  sd 15.2 % (from 7.8 %)
P-L2-5 FAIL  Meng peak 1.95 (from 2.4), y1 ledger +1.08/+1.37, K1 on ramp -0.105: barely moved
bmid+lawq == best+lawq on every metric (9.10 vs 9.19 %) => the b STATE contributes nothing with the container.
Neither registered failure reading applies: honesty fails HIGH, not low. What the figure shows: the
container flips K1(4) hard on every crossing (+0.8 / +0.5 / +0.5) and P44 rides at 0.012-0.020 while
moving, then in the hold q_law = 0, P44 decays to 0.0098 while the across-seed spread stays 0.0133.
Compared with the uniform x3 arm (bias 6.9 %, honesty 0.82, sd 12 %): the shaped container wins
nothing on the target metrics -- the hold, where it injects zero, is where honesty is measured.
Note: Q44_out is logged BEFORE the q_law injection point, so the dotted sqrtQ44 curves coincide.
