# Packaging Decisions Ledger

每個部分完成時記一筆：做了什麼、為什麼、依據哪份 spec/文件。
本帳本是使用者掌握度文件 + 回答 reviewer 提問的依據。
框架層的 12 項定案見 [PACKAGING_PLAN.md](PACKAGING_PLAN.md) §4。

| 日期 | 段/部分 | 決定 | 理由 | 依據 |
|---|---|---|---|---|
| 2026-06-10 | 第 0 段 | 框架定案：目的三層、h_bar>1.2 L2、11 檔結構、controller 單檔 23-state 風格、D6 form、砍 G123、雙 gate 協定、分支策略（D6 在 feat / 打包在 pack） | 理解先於實作；一次只動一個變數；母 repo 與 package 不分岔 | PACKAGING_PLAN.md 全文; kf_canonical_spec.md §1b; LF drift 審計 (test_results/audit_lf_drift_2026-06-10.md) |
| 2026-06-11 | 第 1 段 (D6) | D6 遷移完成並 commit（feat 3cd07c7 + d87909e, merge 2e34308）。h50 5-seed OVERALL PASS（tracking 31.5 nm；bias 2.60/0.63/2.63%；rel-std 2.05–2.54%）。pre/post golden 比對：tracking delta +0.15 nm；**a_hat bias 系統性 +1.8 pt**，歸因實驗（暫時改 Phi*L 單 seed 重跑）分解：gain 位置 L vs Phi*L ~0.4 pt（paper 慣例，spec 預期接受）+ **舊 split form 的 F_e/Q 一步早用 covariance timing 被修正 ~1.4 pt（paper-strict）**。pre-D6 較低 bias 部分來自舊 timing 不一致的偶然有利 | 忠實於 Vpersonal paper-literal form 優先於湊較好看的 bias；delta 全量化、機制明確、gates 全過 | golden_pre_d6 / golden_post_d6 .mat（test_results/pack_baseline；post_d6 標 HEAD d6f95cc 但內容 = 3cd07c7 committed code）；compare_baselines 輸出；spec §1b item 5 |
| 2026-06-11 | 第 1 段簽收 | 使用者驗收通過（檢核題 1 經走讀展開後確認：Φ 推均值 / F_e 推共變異的分工與 D6 變更範圍釐清） | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 第 1 段附帶 | ramp 50→2.7 µm（h_bar=1.2）三 seeds 跑通無數值問題（pre/post 皆然），tracking ~30 nm 全程 | h_bar>1.2 envelope 第一個實證；近壁 dwell 數值穩定（帶 guard 的母 repo 行為） | golden baseline ramp2p7 runs |
| 2026-06-11 | 部分 1（物理世界） | 4 檔 verbatim 抽出（wall_corrections / gamma_inv / thermal_force / step_dynamics），邏輯零改動（例外記錄：step_dynamics local 變數 Gamma_inv→Gi 防撞名；header 全部重寫含 param-flow contract）。三決定執行：params 巢狀形狀保留、thermal seed 鏈 verbatim 保留（保 bit-exact gate 能力）、傾斜壁公式範圍鎖定預設牆向。雙 gate 全 PASS：E1-E4 bit-exact（含 h_bar=1.2 點）、T1-T5 理論（FD<1e-5、Gamma_inv 特徵值、熱變異 <0.6%、MSD 指紋 2.0）。Reviewer 2 WARN 已修：step_dynamics 精度宣告改回 h_bar>=2 驗證範圍（1.2-2 tail 由 ramp gate 接手）、gates 腳本 onCleanup 還原 path/cwd（防 step_dynamics 影子洩漏到 session） | 物理層是純函數，函數級 bit-exact 是最便宜最強的等價證明 | gates_part1.m 輸出；code-reviewer 審查報告 |
| 2026-06-11 | 部分 2（模擬骨架） | 5 檔建立：config.m（取代 user_config+calc_simulation_params，逐欄寫死、無 preset/Bus；**RNG 合約：rng(seed) 後恰好兩個 randi，順序 ctrl.meas_noise_seed 先、thermal.seed 後 = 母 repo calc_simulation_params 呼叫序**）、trajectory_ref（type 2/3 verbatim、osc 砍除）、controller_6state stub（[0]-[11] 骨架 + open-loop bypass）、run_simulation（迴圈 (a)-(k) 忠實、d+1 buffer read-before-shift、unit-delay IC=p0、driver 層 thermal gate）、gates_part2。雙 gate 全 PASS：G1 params 逐欄相等含兩個 randi seed（三場景）、G2 軌跡 bit-exact、**G3 開迴路全模擬 bit-exact（全時序 max diff = 0）**、T1-T3 理論。Reviewer 2 WARN 處置：h10 補進 G1/G2（已修）；run_simulation 與母 repo test_script 同名 script 的 cwd 陰影風險 → 保留名稱（剪臍帶後為自然命名）+ 記錄。G3 路徑隔離改對稱（兩側各自 rmpath 對方）。Stub ekf_out 佔位值（zeros vs 母 [1;1;1;0]）部分 3 定案。延遲鏈 k-表驗證移至部分 3（需控制器 delta_x_m log） | RNG 鏈與迴圈次序是 bit-exact 的全部前提，G3 全零差 = 兩者皆對的鐵證 | gates_part2 輸出；code-reviewer 審查報告（randi 順序獨立查證 calc_ctrl_params:28 / calc_thermal_params:19） |
| 2026-06-11 | 部分 3（量測鏈） | controller_6state 升級為量測鏈中間態：[0] init 子集（C_dpmr/C_n full a_pd 閉式內嵌、wall-aware a_x 種子、IIR prefill、pd buffers）、[1] TEMP f_d=0、[2] delta_x_m + IIR 四行 verbatim + a_xm、[11] shifts + INTERIM z 軸探針 ekf_out。R 改名 R_radius（避開部分 5 量測共變異 R 撞名）。gates_part3 全 PASS：C1 常數雙源 bit-exact（3.1610/1.1093）、C2 延遲 k-表 4798 步全等 + IC k=1-3 + prefill、C3 合成 AR(1) 濾波理論 emp/theory=1.0038 + a_xm 反解 bit-exact、C4 d=2。gates_part2 regression 無退化。**兩個被 gate/審查抓到的真缺陷**：(a) reviewer BLOCKER — 探針原在 x 軸而 ramp 只動 z 軸（w_hat=[0;0;1]），pd 側檢查是空集 → 換 z 軸探針；(b) gate 實跑 FAIL 抓到 log-row 索引真相 — **p_m_out 第 j 列存的是第 j 步積分後的量測（樣本 j+1），k-表正確形式 = p_d_out(k-2) − p_m_out(k-3)**；樣本域仍是乾淨 d=2（兩項都在樣本 k-2），列偏移純屬 logging 慣例（審查與實作者最初都推錯，被實跑修正） | k-表 gate 的存在理由就是釘死時序真相；INTERIM 探針（部分 5/7 換成 a_hat_xyz）的 churn 是換取「延遲鏈現在就有鐵證」的代價 | gates_part3 輸出；code-reviewer 報告（transcription fidelity A-E 全 PASS） |
| 2026-06-11 | 部分 4（控制律） | Eq.17 ACTIVE form verbatim 填入（六項 bracket、Σâ[k−i]f_d[k−i] 過去力以過去增益加權、d=2 兩項式）；f_d/â 歷史 buffer 實作（init 0 / a_x_init，[11] shift）。**骨架順序修正：[1] 量測鏈、[2] 控制律**（Eq.17 回授當步 raw δx_m，必須量測先行 = 母 repo 真實資料流；23-state 控制律先行是因為它用估計值）。TEMP 插樁：â 凍結 a_x_init、x̂D=0（= 完美知識，部分 5-6 EKF 進來後換 a-priori extraction）。gates_part4 全 PASS：**D1 合成離散 plant 完美知識 λc 衰減精確（max dev 3.09e-11，手推 telescoping 確認 k≥2 起精確成立）= Eq.17 Σf_d 延遲補償代數的端到端鐵證**；**D2 第一次閉迴路 tracking [31.7 32.3 30.1] nm**（h50 全噪聲，凍結完美 â）。Reviewer APPROVE：control law 轉寫 bit-identical、buffer timing 與母 repo 同構、a_true==a_x_init bitwise 抵消成立。已修：D1 window 浮點下限註解、driver 加 d==2 assert（防靜默失配）、stale 標籤。**待拍板（部分 5 討論）：D1 的去向** — EKF 落地後 â 不再精確等於 a_true，D1 會 FAIL；選項 = 改成同條件下 vs 母 controller 的等價形式 / 標記為 part-4 snapshot gate | Eq.17 的核心主張（自帶 d-step 延遲補償）值得一個精確代數 gate 而非統計 gate；量測先行的順序修正記錄了與 23-state 模板的結構性差異原因 | gates_part4 輸出；code-reviewer 報告（A-E 全 PASS，含 D1 獨立手推） |
| 2026-06-11 | 部分 5（EKF 遞迴 D6） | controller_6state 全檔重寫：EKF 心臟 [3]-[10] 落地（D6 predictor cycle Eq.19→16→20 Joseph→21、Φ≠F_e、per-axis 6x6）+ [0G] DARE a-priori 化 init + Q[k]/R[k]（部分 6 驗內容物）。四個 local function 內嵌（compute_if_abc/if_eff_eval/build_F_e/solve_dare_kf），constants builder 併入 [0]，IF 自檢從 warning 升 assert。**無 guard**（H 恆 2x2、無 K 凍結、無 R_OFF = 決定 6 落實）。INTERIM 探針退役 → ekf_out 最終形 [a_hat_xyz; h_bar]；C2/C3/D1 轉 snapshot（控制律/量測鏈節文字凍結）。gates_part5 全 PASS：E1 DARE 定點 residual<1e-11 + 後驗 PSD + 先驗 P_f0 PD；**E2 h50 live EKF 5-seed tracking [31.5 31.2 31.5] nm / bias [2.60 0.63 2.63]% / rel-std [2.04 2.38 2.53]% = 與母 repo D6 golden baseline 逐位元相同**；E3 ramp h̄=1.2 無 guard 數值穩定 + tracking 30 nm + a_hat 全程正（近壁準確度不 assert = L2 限制）。全套 regression（part2-4）乾淨。Reviewer F1（量測鏈域保護 enable_wall && isfinite && h_bar>1 被簡化）已還原；F2 bypass sentinel [1;1;1;0] 對齊母 repo；其餘 stale 標籤已修 | EKF 是 package 心臟，逐節對 spec §1b/§3/§5/§6；E2 與母 repo 數字相同是 fidelity 的最強即時證據（part-7 正式 bit-exact） | gates_part5 輸出；code-reviewer 報告（A-H transcription faithful，loop fusion numerics-neutral 證明） |
| 2026-06-11 | 部分 6（Q/R 驗證輪） | **不改 code**，新增 gates_part6.m 驗 Q/R 公式建構正確性。F1 Q55 增量因子精確代數（dev 2.2e-16）、**F2 合成理想閉迴路 ground truth**（Var(dx)/C_dx=1.0018、Var(incr)/[2/(1+lc)]=1.0008、rho1 0.8516 vs 0.8515 = Q55 整條推導鏈）、F3 IF 自洽（Method A==B 同式分組 + self-check）、F4 C 常數（a_pd→0→3.96/1.18）、**F5 真實閉迴路 ground truth**（外部重建 IIR 殘差 vs C_dpmr·4kBT·a_z+C_n·σ²_nx，emp/closed=0.9539）。**Reviewer 誠實評估採納**：F1/F3/F4 被點出是一致性/drift 防護非獨立驗證（F3 dual-computation 其實是恆等式分組+重用 controller compute_if_abc）→ header 改誠實標註 + 補 F5 真 ground truth（reviewer 指 C_dpmr/C_n 缺物理錨點）。F2+F5 承載獨立驗證 | gate 即交付物，數學正確性與「測真東西」是重點；誠實標註避免誤導 reviewer | gates_part6 輸出；code-reviewer 報告（無 BLOCKER、F2 強、建議補 ground truth→已補 F5） |
| 2026-06-11 | 部分 8（驗證層 + 交付） | 新增 4 檔：verify_standalone（三場景，h50 量化 PASS gate、h10/ramp 描述性，自足無母 repo 依賴）、make_figures（2 圖 a_hat vs a_true + tracking，僅用 logged 訊號因無 diag；檔名帶 scenario 防覆蓋）、main_run（單一入口 addpath+run+figures）、README（reviewer 地圖：框架圖/閱讀順序/11 步/三個「不是 bug」預警/L2 envelope/notation 表）。功能測試全通過：h50 OVERALL PASS [31.5 31.2 31.5] nm bias [2.60 0.63 2.63]%；h10/ramp tracking 30nm 但 a_hat 近壁退化（h10 bias 5.2%、ramp rel-std 15.9%）= L2 實證。18 檔 checkcode 0 issue。Reviewer：part-8 logic PASS 無 blocker；**WARN 2 已修**（README §6「bias ~1%」是 pre-D6 stale → 改 ≲3%/2.6% 並註 D6 +1.8pt）；WARN 3 圖檔名帶 scenario；BLOCKER（cut-loose）= README 已區分自足 gate(4/5/6+verify+main) vs 母 repo 比對 gate(1/2/3/7)，實際歸檔留剪臍帶步驟；NIT 6/7 修（t_hold 讀 params、移除 rng no-op） | verify/figures/main 是 reviewer 的入口三件；README 是審查地圖，講解 prose 標 ✎ 待使用者定稿（掌握度協定第二件親手工作） | verify_standalone 輸出；code-reviewer 報告（A-E + cut-loose readiness，核心自足性確認） |
| 2026-06-11 | 部分 7（閉迴路等價） | **不改 code**，新增 gates_part7.m = package 招牌等價證明。**先實測再定門檻**：閉迴路 h50 對母 repo 非 bit-exact，差 ~3 ulps（f_d max rel 6.9e-16、F_th/p_d 零差），純 IEEE-754 結合律差異（已定位 R22 加法 A+(b+c) vs (A+b)+c）、不放大（收斂閉迴路）、低於追蹤精度 9 數量級。**使用者拍板 rounding-floor 等價（門檻 rel<1e-12，保 code 乾淨不追 bit-exact）**。H1 h50 3-seed max rel 6.92e-16、H2 h10 3-seed 3.41e-14（含 a_hat 直接比對，axis reorder [1 3 2 4]）、**H3 ramp 邊界**：h̄>1.5 rounding-floor 等價 4.42e-14、**h̄<1.5 a_hat_z 發散 38%**（母 repo G3 gate y_2、package 不 gate = L2 邊界定量實證）、standalone 全程穩定 tracking 30 nm。Reviewer 判 H1-H3 SOUND 無 blocker；WARN 已修：run_pair 母 config 改顯式自足（不依賴 user_config 預設防未來 drift）+ a_hat 直接比對 + H3 mask 用 min(兩邊 h̄) + Ts 讀 meta | package 與 production code 的差異被定量定位在唯一一處（近壁 a_hat），其餘全 rounding-floor 相同 = 對 reviewer 最強的「忠實且範圍明確」陳述 | gates_part7 輸出；code-reviewer 報告（path 隔離 fail-loud、RNG 順序與 axis mapping 逐一查證、無 spurious-pass channel）；先前 probe b92exzcui |
| 2026-06-11 | 部分 6 簽收 | 使用者驗收通過（檢核題 2 經走讀展開：F3 = consistency guard 非 correctness proof；Method A==B 是恆等式分組、且重用 controller compute_if_abc 故非獨立；留它擋的是「本該一致的兩段 code 漂移」—— self-check 綁 C_dpmr 閉式 vs transfer-function model、Method A==B 抓重構打錯字、poles 跑出 (0,1)。核心一課：gate PASS 要分清驗「正確性」還是「一致性」） | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 部分 5 簽收 | 使用者續行（走讀檢核題:無 guard 不影響 h50 因 guard 在 h50 不觸發；E3 不驗近壁準確度對應 L2 envelope 宣告） | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 部分 4 簽收 | 使用者驗收通過，續行部分 5 | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 部分 5 前置決定 | (1) 部分 5+6 程式碼一次落地（EKF 遞迴與 Q/R 執行上不可分），驗證/走讀仍分兩輪；(2) INTERIM 探針退役，ekf_out 換最終形 [a_hat_xyz; h_bar]；建構期 gates C2/C3/D1 轉 snapshot（已建立的事實不變，控制律/量測鏈節文字凍結），繼任者 = 部分 7 閉迴路 h50 bit-exact vs 母 repo（h50 下母 guard 全程不觸發，理論可全零差）；(3) EKF 無 guard：H 恆 2x2、無凍結、無 R_OFF（決定 6 的落實點） | 探針只有 4 slots，EKF 落地後讓位給 a_hat；snapshot gate 的價值在歷史證明 + 文字凍結警示 | 部分 4 reviewer Finding 2；PACKAGING_PLAN 決定 6 |
| 2026-06-11 | 部分 3 簽收 | 使用者驗收通過（檢核題 1 prefill 經走讀展開：閉迴路穩態公式、無 prefill 的暖機毒性 a_xm<0、三柱 init 中取代 G1/G2 的源頭治療角色、開迴路中間態的誠實註記） | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 部分 2 簽收 | 使用者走讀完成（config.m 內部五段流程 + RNG 合約 + 兩腳本用意：stub = 介面凍結/施工藍圖/G3 啟用/防誤用，gates = 永久 regression 考卷）。檢核題 1（區塊對調 → seed 互換 → G1/G3 抓到）與題 2（G3 證機構、k-表驗對位需部分 3 的 delta_x_m log）確認 | 走讀協定 | 對話紀錄 session "pack" |
| 2026-06-11 | 部分 1 簽收 | 使用者驗收通過（三題標準答案對照一致）。追加問題「真的測過隨機性嗎」→ 誠實回答只測了可重現性+量級 → 新增 T6 隨機品質 gate（白性 lag1-10 max rho 0.0058 / 軸間獨立 / 高斯動差 / 雙 seed 獨立，全 4 sigma 界內）PASS | 隨機品質從「繼承母 repo G1-G7 證據」升級為「直接測試」 | gates_part1 T6 輸出 |
| 2026-06-14 | 收尾 9a（main_run 腳本化 + 設定區塊） | main_run 從 function 改 **SCRIPT** + 頂部「SIMULATION SETTINGS」8 旋鈕（scenario/seed/T_sim/lambda_c/a_pd/a_cov/meas_noise/thermal），使用者改值按 Run、不打指令。接縫：**config 加選用第二參數 `overrides`**（向後相容；nargin/isfield 守衛；在 params builder **之前**套到 scalar tunables → RNG 合約「恰兩個 randi、順序 ctrl 先 thermal 後」不受影響），**run_simulation 加 `opts.overrides` 轉傳 config**。T_sim 走 config override → 同時進 cfg.T_sim（迴圈長度）與 params.common.T_sim（ramp 降速 auto-fit）。seed=[] → `rng('shuffle')` 挑隨機種子並印出（實際跑仍 `rng(seed)` 可重現）。順帶補缺口：每跑把 `out`+`settings` 存 `.mat`（**save 在畫圖之前**，畫圖失敗不丟結果）。meas_noise_std 不露出（留 config）。**軌跡參數 + 輸出圖延後（§9c）**。驗證：3 檔 checkcode 0 issue；verify_9a 8/8 PASS（向後相容 / RNG 兩 seed 不動 / override 生效 / thermal·meas_noise off / lambda_c 改 f_d / T_sim 改長度）；main_run 端到端跑通（隨機 seed 印出、tracking ~31 nm、.mat+figs 存）；gates_part4 regression PASS。Reviewer 無 BLOCKER、八項 invariant 全 CONFIRMED；採納 WARN#1（save 移到 figures 前）+ 種子守衛 + overwrite 註記；NIT base-workspace 洩漏 = edit-and-run script 固有取捨，接受 | 使用者要在 code 裡改設定不打指令；config 是完整定義/預設、main_run 是薄殼覆蓋層；RNG 合約是 bit-exact 等價前提不可動 | PACKAGING_PLAN §9a；verify_9a 輸出；code-reviewer 報告（agentId a0abe3f3，invariant 全 CONFIRMED）；§9b/§9c/§9d 仍待辦 |
